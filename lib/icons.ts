/**
 * One icon family for the whole project (Section 3.C): Phosphor, nothing else.
 * Weight is standardised here so no component picks its own.
 */
export const ICON = {
  weight: "regular",
} as const;
